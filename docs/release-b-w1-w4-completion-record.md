# Demohub Release B — W1–W4 completion record (2026-09-18)

Answers `Demohub-Execution-Results-2-Review-and-Claude-Handoff-2026-09-18.md`. Per that review this is **not** a request for another review cycle: it is the record of the four work items, to be folded into the single closure note after the cutover. Application candidate unchanged: `67613b5b1f7bb4ca1d69c94fb6eb26f8e367180d` (tree `a17c8054…`); the branch head `0488592` adds only empty Preview-rebuild commits (`git diff 67613b5..HEAD` empty). **Production untouched.**

| Item | Result | Evidence (demohub-docs) |
|---|---|---|
| **W1** old staging key retired | The exposed key (`local_dev`, new-style `sb_secret_`) deleted by David. Before: old 200 / new 200. After (2026-09-18T20:47Z): **old 401 `Unregistered API key`**, new 200; Preview binding healthy. Replacement CI evidence unchanged (run 35291726833). | `evidence/cutover/2026-09-18-W1-old-key-revocation.md` |
| **W2** Preview routing + browser + mail | `SITE_ORIGIN` split: Production row value untouched, new **Preview** row = the protected preview URL. Preview now accepts only its own Origin (production origin → 403); CSRF and Deployment Protection unchanged. **Real-browser journey 23 passed / 0 failed**: magic-link sign-in → profile save → COI upload (pending review) → UI booking → agreement modal → Stripe hosted test authorization → **Stripe returned into the Preview** (`/r/<slug>?paid=1&held=1` → `/brand/dashboard?booked=1&held=1`), dashboard "Slot held … not been charged" == backend (`held`/`authorized`) == Stripe (`requires_capture`, $5.00 capturable, $0 received). On-the-wire headers of every API mutation: `Origin` = preview, `Sec-Fetch-Site: same-origin` — nothing forged. **Mail: received `[SINK]` messages in the operator's allowlisted mailbox**, each showing its rewritten intended recipient, correlated to the browser journey's plus-address and to the HTTP journey's fixtures (confirmed delivery to the sink, not just provider acceptance). | `evidence/preview-journey/2026-09-18-preview-browser-journey.md`, `…-browser-*.png`, `preview-browser-journey.mjs`, `2026-09-18-W2-mail-containment-record.md`, `2026-09-18-sink-inbox-operator-mailbox.png` |
| **W3** hardened wrappers | Guard = exactly one identity row AND `environment='production'` AND `project_ref='dkgjvsstbgnhcfboqqnd'`; exact ordered list of all ledger versions ≥ 0060 (0073 absent; missing / extra / duplicate / null / out-of-order refused, null-safe); committed-marker refusal for 0074 (`bookings.start_at` or either notification table), 0075 (`venues.availability_version`), 0076 (`booking_slot_start_strict(date,text,text)`), 0077 (`booking_interval_ok(date,text,integer,text)`); builder asserts every explicit-COMMIT body has a marker and only those. Ledger INSERT stays last, no conflict-ignore. `00-preflight.sql` = the complete §3 gate in one result set (identity both fields, exact history, all A rows, per-job heartbeats with outcome and after-T1 flag, fulfilment/event/booking touches vs T1, non-completed event detail, app DB activity, retained-data counts) with a mandatory T1 literal; Vercel invocation drain and Stripe delivery checks remain required outside SQL. Bodies read with `git show 67613b5:…`; migration and generated-file sha256 frozen. | `evidence/cutover/make-cutover-kit.mjs`, `cutover-kit-MANIFEST.json`; kit in `Documents/Codex/cutover-kit/` |
| **W4** wrapper tests | 16 scenarios run **in the dashboard SQL editor** of demohub-rebuild-check against a scratch schema, same `wrap()` generator, in-page hash of each executed text == manifest; state read over a fresh connection after each. Wrong environment / wrong project / two identity rows / missing version / unexpected 0073 / duplicate / wrong predecessor → all stop before any write. Fail before COMMIT → nothing committed. Fail after COMMIT → marker present, no ledger row → **re-paste refused**. Implicit-batch failure → migration and ledger insert rolled back together. Success records one version; repeat refused. No open failed transaction after any deliberate error. Scratch schema dropped; real identity/ledger untouched. Dashboard behaviour matched every assumption. | `evidence/cutover/2026-09-18-W4-wrapper-test-results.md`, `wrapper-tests-MANIFEST.json` |

## Evidence labels corrected (your §"Correct the evidence labels")

1. The deployed error scenario proves **authorization cancelled before confirmation → truthful `capture_failed`**. Captured-but-unapplied and unknown-outcome paths are covered by the previously accepted fault-injection suites (in-process, real database), not by a deployed test.
2. The journey's notification check shows **convergence to a terminal outbox state**, not semantic exactly-once delivery.
3. The overlapping sweep: safe convergence **in the tested case**; the losing invocation's 500 stays visible. A 15-minute schedule is not an exclusivity guarantee; persistent failures, unresolved holds or new cases require operator attention.

## Disclosed, not blocking

- Two other pre-rotation secret keys on the **test** project (`github_actions`, `vercel_preview`) were never exposed; both consumers were moved to the rotated key on 2026-09-17. Whether David also deleted them is **not confirmed**.
- Grassroots Demos Stripe **test** key: David's accepted-risk exception stands; absent from every active Demohub binding (verified by `GET /v1/account` on the keys in use). Production live-mode account ownership is to be confirmed by David in the Demohub dashboard, not inferred from id patterns.
- Vercel **Ignored Build Step** is `Automatic` for the validation; restore `Only build production` after launch (docs-branch pushes currently also build previews).
- UI observation, no change made (copy is out of scope): with a certificate uploaded but not yet reviewed, the brand dashboard's Overview tile reads "No COI on file" while the Compliance tab correctly reads "Pending review".

## What remains

David's authorization of the window; then runbook v5.1 with the hardened kit: contain (flags off on the old build, operator freeze, cron off = T0, drain to T1 with the recorded max function duration and Vercel logs) → `00-preflight.sql` with T1 + Stripe deliveries check, twice → ten pastes → deploy `67613b5` with intake off → `11-final-verify.sql` + SHA/bindings/`/gussmarket`/retained data → workers on, one succeeded heartbeat each on the new build → intake on → one closure note.
