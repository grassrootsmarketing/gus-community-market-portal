# Live payment smoke — production, 2026-09-19

- Authorization: David, in chat — "alright lets run the payment test" (own card, internal test retailer, $1). 00:54:41Z
- 00:54:43Z setup: internal retailer `zz-demohub-live-smoke-0919` (keeps-all, manual confirm, **0 store contacts**), venue "Live Smoke Venue ($1)", brand = David's own email, one-time sign-in token.
- 01:13:31–01:13:33Z **HOLD** — David booked through the live UI (no COI on file; the form showed "temporary hold (not a charge)") and paid on Stripe's hosted page with his own card. **Stripe's live webhook delivered** `payment_intent.amount_capturable_updated` and `checkout.session.completed` (both `completed` in the event inbox). Ledger: booking `held` / `authorized`, `held_expires_at` = +24 h; allocation $1.00 (venue 100, platform fee 0); group `authorized`; held-stage fulfilment generation 1 **done**, emails sent; **no demo**, no refund rows, no reconciliation case.
- Note: David did not use the pre-created brand; he went through the **real brand signup on the live site** with a plus-address of his own mailbox (brand "Test Name #1", created 01:12:15Z) and booked as that brand — so signup + email verification on the new build were exercised too. The test state now follows that brand. **Hold email received** (David's inbox screenshot, 6:13 PM PT): from `bookings@demohubhq.com`, "Your demo slot is held … We placed a hold on your card for $1.00 — you have not been charged", store "Live Smoke Venue ($1)", Friday October 9, 2026, 9:00 AM — a real production email, no sink rewriting, amount and wording correct.
- 01:16Z David uploaded the sample COI in the live dashboard → "Pending review" (AI auto-check is off in production by design: "could not auto-check … the retailer will review it").
- 01:17:11Z **owner approval** through the live `owner-coi-review` route (owner session minted for david@demohubhq.com): 200 approved. Manual-confirm retailer → **no capture** on approval (booking still held/authorized, 0 demos, 0 cases).
- 01:17:30Z **CAPTURE** — retailer confirm through the live `booking-action` route: 200 `{ok:true, demo_id, email_sent:true}`. Stripe's live `payment_intent.succeeded` delivered and `completed`. Ledger: booking `confirmed` / `paid`; group `paid` $1.00; fulfilment re-issued as **generation 2**, done, emails sent; **exactly one demo** (confirmed, 1 h); no refund rows; no reconciliation case.
- 01:17:53Z **REFUND** — retailer cancel through the live route: 200 `{refund_status:"submitted", demo_cancelled:true, email_sent:true}`. Stripe's live `charge.refunded` + `refund.created` delivered and `completed`. Ledger: booking `cancelled` / `refunded` (refund id set); group `refunded`; allocation customer 100 / **refunded 100**; refund request $1.00 **succeeded** with a Stripe refund id, 0 retry attempts; demo `cancelled`; no reconciliation case.
- Post-smoke audits (read-only RPCs): `projection_anomalies` 0, `snapshot_drift` 0, `offering_anomalies(invariant)` 0, `schedule_mismatches` 0; open reconciliation cases 0; site status `operational`. Notification outbox: one delivery, `owner_booking_created` → owner, `accepted`; no store-contact mail (the test retailer has no store contacts; Gus's contacts were not involved).
- Net money: $1.00 authorized → captured → fully refunded to David's own card. Not verified from the Stripe side by Claude (no live key held); ledger + Stripe-delivered webhooks are the evidence, David can confirm in the Stripe dashboard.
- Left in production on purpose (real financial records are not deleted): the internal retailer `zz-demohub-live-smoke-0919`, its $1 venue, two brand rows under David's own mailbox ("Live Smoke Brand (David)" unused, "Test Name #1"), one booking/payment group/refund/demo (all terminal). Disposition is David's call.
